spring:
  datasource:
    url: jdbc:h2:~/data/cafeteria
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---

spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:test

---
